This is a small module, containing only some helper functions to configure AVR hardware features.

For example, the file timers-atmega168.c has a function to get prescale bits from a given prescale factor.

